// interface Props {
//   update: UpdateFn;
// }

export function RockParameters() {
  // const [state, setState] = useState<RockParams>();

  // useEffect(() => () => update({ params: state }), [state]);

  return <p>Rock Parameters</p>;
}
