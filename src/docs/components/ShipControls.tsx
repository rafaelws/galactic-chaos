// TODO Ship:
// Movement (reusable =>
//   linear, quadraticBezier, cubicBezier, repeatable
// )
// Fire (reusable)
// *Impact (reusable)

export function ShipControls() {
  return <p>Ship Controls</p>;
}
